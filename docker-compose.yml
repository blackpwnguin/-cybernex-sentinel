version: '3'

services:
  cybernex:
    build: .
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - FLASK_ENV=development
    command: python backend.py
    restart: always
